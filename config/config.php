<?php

return [

    /*
     * Path where the generated playbooks will be placed.
     * If it starts with a \ (ie: \Domain\Support\Playbooks) it will follow that namespace.
     * Otherwise (ie: Playbooks) they will be put into the root namespace (the default \App)
     *
     * default: 'Playbooks'
     */
    'default_path' => 'Playbooks'
];